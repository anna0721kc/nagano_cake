<!--トップページ/会員側-->
<!--<%= image_tag 'PS_KCT_0915L.jpg', class: 'w-100 h-25' %>-->
<div class="main-visual">
  <div class="main-visual-content">
        <p class="main-visual-text">
          ようこそ！ながのCAKEへ！<br />
          このサイトは、ケーキ販売のECサイトになります。<br />
          購入には会員登録が必要になります。
        </p>
        </div>
</div>
<div class="container my-5">
  <div class="row">
    <h3>新着商品</h3>
  </div>

  <div class="row mx-auto mt-3">
    <% @items.last(4).each do |item| %>
      <div class="mx-auto">
        <%= link_to item_path(item) do %>
          <%= attachment_image_tag item, :image, :fill, 100, 100, format: 'jpeg', fallback: 'noimage.jpg', size: '200x200', class: "rounded" %><br>
        <% end %>
        <div class="row mt-3">
          <span class="px-3"><%= item.name %></span>
        </div>
        <div class="row mt-1">
          <span class="offset-8"><%= item.with_tax_price.to_s(:delimited) %> 円</span>
        </div>
      </div>
    <% end %>
  </div>
</div>